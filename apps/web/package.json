{
  "name": "@remoteos/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @remoteos/contracts build && vite build",
    "dev": "pnpm --filter @remoteos/contracts build && vite",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @remoteos/contracts build && vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@remoteos/contracts": "workspace:*",
    "@remoteos/ui-web": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "@vitejs/plugin-react": "^4.4.1",
    "typescript": "^5.8.3",
    "vite": "^6.2.0",
    "vite-plugin-pwa": "^1.0.0",
    "vitest": "^3.0.7"
  }
}
